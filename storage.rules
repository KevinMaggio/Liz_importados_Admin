rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Reglas temporales para desarrollo - PERMITIR TODO
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
} 